\begin{figure}
    \centering
    \includegraphics[width=0.96\textwidth]{final/analysis/latency_with_stage}
    \caption{
        CPU上每层中最快的模块是不同的。
        X轴是每层输入特征图的尺寸：$H \times W \times C_{in}$。
        模块的配置是：$K=3$，$S=1$。
    }
    \label{fig:block_latency_cpu}
\end{figure}